# Historical Phase 2B Fixtures

This directory is retained as historical fixture data for pre-unification command-surface examples.

It is not a second active runtime or a current program phase. The canonical live runtime is `src/avn/`, and governed sweeps should run through:

```bash
python -m avn adaptive-sweep <manifest.json>
```

The historical TOML fixtures that reference this directory now live under `archive/legacy_runtime/configs/` and are retained for provenance only.
